#pragma once
#include <string>
#include <vector>

namespace util {
namespace stringutils {

  std::vector<std::string>
  Split(const std::string &source, const char &delimiter);

} // namespace string
} // namespace utilities
